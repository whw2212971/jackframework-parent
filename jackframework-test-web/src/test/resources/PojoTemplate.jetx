package ${pojoInfo.packageName};

#for (String pkgName : pojoInfo.imports)
import ${pkgName};
#end

public class ${pojoInfo.pojoName} {
    #for (tools.PojoGenerationTools.FieldInfo fieldInfo : pojoInfo.fields)

    private ${fieldInfo.fieldType} ${fieldInfo.fieldName};
    #end

    #for (tools.PojoGenerationTools.FieldInfo fieldInfo : pojoInfo.fields)
    public void ${fieldInfo.setterName}(${fieldInfo.fieldType} ${fieldInfo.fieldName}) {
        this.${fieldInfo.fieldName} = ${fieldInfo.fieldName};
    }

    public ${fieldInfo.fieldType} ${fieldInfo.getterName}() {
        return ${fieldInfo.fieldName};
    }

    #end

}